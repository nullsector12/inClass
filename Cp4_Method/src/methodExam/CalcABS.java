package methodExam;

public class CalcABS {

	// 속성 = 숫자, 숫자를 전달받는다.
	int num1;
	int num2;
	
 public void changeABS(int a, int b) {
	a = Math.abs(num1);
	b = Math.abs(num2);
	System.out.println("num1의 절대값 : " + a + "\n" + "num2의 절대값 : " + b);
}

	
	
}
